USAGE: scheme --load "interp.scm"
Once scheme loads interp.scm, can be used as a normal scheme interpreter. 

(load FILENAME) 
      Loads FILENAME in a read-eval loop. 

exit
      Exits interpreter. 

